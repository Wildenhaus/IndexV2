<UserControl x:Class="Index.Modules.TextureEditor.Views.TextureInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Index.Modules.TextureEditor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border>
        <Grid x:Name="InfoGrid"
              Margin="5">

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="10"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

        </Grid>

        <Border.Style>
            <Style TargetType="{x:Type Border}">
                <Setter Property="Background" Value="#A0000000" />

                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#D0000000" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
    </Border>

</UserControl>
